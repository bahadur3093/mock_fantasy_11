import { Text, View } from "react-native";

const SeasonsPage = () => {
  return (
    <View className="h-full relative bg-orange-300">
      <Text>Seasons page</Text>
    </View>
  );
};

export default SeasonsPage;
